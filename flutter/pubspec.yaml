name: meow_meow
description: Meow Meow - Friend & Chat Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Supabase
  supabase_flutter: ^2.3.0

  # State Management
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3

  # Navigation
  go_router: ^13.0.0

  # Network
  dio: ^5.4.0
  connectivity_plus: ^5.0.2

  # Local Storage
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

  # UI Components
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_spinkit: ^5.2.0
  lottie: ^2.7.0
  photo_view: ^0.14.0

  # Forms & Validation
  flutter_form_builder: ^9.2.1
  form_builder_validators: ^9.1.0

  # Utilities
  intl: ^0.18.1
  uuid: ^4.2.2
  equatable: ^2.0.5
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  logger: ^2.0.2+1
  timeago: ^3.6.1

  # Media
  image_picker: ^1.0.7
  image_cropper: ^5.0.1
  permission_handler: ^11.2.0
  
  # 1:1 calls — open-source P2P WebRTC (Google libwebrtc, BSD)
  flutter_webrtc: ^0.10.8

  # Group calls — open-source SFU client (LiveKit, Apache 2.0)
  livekit_client: ^2.2.4

  # Native screenshot protection (Android FLAG_SECURE / iOS captured detection)
  screen_protector: ^1.4.2

  # Notifications
  flutter_local_notifications: ^16.3.2
  firebase_messaging: ^14.7.10
  firebase_core: ^2.24.2

  # Icons
  cupertino_icons: ^1.0.6
  lucide_icons: ^0.254.0

  # File export & sharing (wallet statement CSV)
  share_plus: ^7.2.2
  path_provider: ^2.1.2

  # Ringtone playback for incoming calls
  audioplayers: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  riverpod_generator: ^2.3.9

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
    - assets/i18n/

  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/Poppins-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Poppins-Bold.ttf
          weight: 700
