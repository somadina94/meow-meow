// Kept for backward-compatibility imports. Toasts now render through sonner only.
// This component is a no-op — the sonner <Toaster /> in App.tsx handles all rendering.
export function Toaster() {
  return null;
}
